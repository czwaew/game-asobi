const $=id=>document.getElementById(id);let tasks=JSON.parse(localStorage.jaTasks||'[]');let memos=JSON.parse(localStorage.jaMemos||'[]');function tick(){const d=new Date();$('clock').textContent=d.toLocaleTimeString('ja-JP',{hour:'2-digit',minute:'2-digit',second:'2-digit'});}setInterval(tick,1000);tick();function save(){localStorage.jaTasks=JSON.stringify(tasks);localStorage.jaMemos=JSON.stringify(memos)}function render(){$('tasks').innerHTML=tasks.map((t,i)=>`<li class="${t.done?'done':''}"><span onclick="toggle(${i})">${t.text}<br><small>${t.time}</small></span><button onclick="delTask(${i})">削除</button></li>`).join('');$('memos').innerHTML=memos.map(m=>`<div class="memo"><b>${m.member}</b><br>${m.memo.replace(/\n/g,'<br>')}<br><small>${m.time}</small></div>`).join('')}function addTask(text){tasks.unshift({text,done:false,time:new Date().toLocaleString('ja-JP')});save();render()}function addCustomTask(){const v=$('taskInput').value.trim();if(!v)return;addTask(v);$('taskInput').value=''}function toggle(i){tasks[i].done=!tasks[i].done;save();render()}function delTask(i){tasks.splice(i,1);save();render()}function clearDone(){tasks=tasks.filter(t=>!t.done);save();render()}function saveMemo(){const member=$('member').value.trim()||'未入力';const memo=$('memo').value.trim();if(!memo)return alert('メモを入力してください');memos.unshift({member,memo,time:new Date().toLocaleString('ja-JP')});$('member').value='';$('memo').value='';save();render()}function exportCSV(){const rows=[['種類','名前/内容','詳細','日時'],...tasks.map(t=>['タスク',t.text,t.done?'完了':'未完了',t.time]),...memos.map(m=>['メモ',m.member,m.memo,m.time])];const csv=rows.map(r=>r.map(v=>'"'+String(v).replace(/"/g,'""')+'"').join(',')).join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob(['\ufeff'+csv],{type:'text/csv'}));a.download='ja_work_data.csv';a.click()}function scrollToTop(){scrollTo({top:0,behavior:'smooth'})}if('serviceWorker'in navigator)navigator.serviceWorker.register('sw.js');render();
